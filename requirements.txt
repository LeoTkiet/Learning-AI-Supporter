# ==============================================================================
# HỆ THỐNG HỖ TRỢ HỌC TẬP ĐA MÔN ÁP DỤNG AI (TOÁN, LÝ, HÓA)
# Danh sách thư viện Python tổng hợp (Backend FastAPI & Streamlit Hub)
# ==============================================================================

# -----------------
# 1. Backend Web Framework & Server
# -----------------
fastapi>=0.111.0            # RESTful API framework hiệu năng cao
uvicorn[standard]>=0.30.0   # ASGI web server
pydantic>=2.7.0             # Data validation & type checking
pydantic-settings>=2.2.0    # Quản lý cấu hình biến môi trường
python-multipart>=0.0.9     # Xử lý upload file ảnh bài làm (Form/Multipart)

# -----------------
# 2. AI Core (Google Gemini API)
# -----------------
google-generativeai>=0.7.0  # SDK chính thức gọi mô hình Google Gemini 1.5
google-genai>=0.1.1         # Google GenAI modern client

# -----------------
# 3. OCR Pipeline & Image Preprocessing
# -----------------
opencv-python-headless>=4.9.0.80 # Tiền xử lý ảnh (Lọc nhiễu, Grayscale, Adaptive Thresholding)
pytesseract>=0.3.10         # Wrapper Python cho engine Tesseract OCR
pillow>=10.3.0              # Xử lý và đọc định dạng ảnh (PIL)
numpy>=1.26.0               # Tính toán ma trận điểm ảnh

# -----------------
# 4. Database & Auth (Supabase PostgreSQL)
# -----------------
supabase>=2.4.0             # Supabase Python Client (Auth, PostgreSQL, Realtime)
python-jose[cryptography]>=3.3.0 # Xử lý mã hóa JSON Web Token (JWT)
passlib[bcrypt]>=1.7.4      # Băm mật khẩu người dùng an toàn

# -----------------
# 5. Streamlit Dashboard & Data Visualization
# -----------------
streamlit>=1.36.0           # Framework dựng nhanh dashboard kiểm thử nội bộ
plotly>=5.22.0              # Vẽ biểu đồ Radar năng lực và đồ thị tương tác
pandas>=2.2.0               # Xử lý dữ liệu bảng xếp hạng và ngân hàng câu hỏi

# -----------------
# 6. Utilities & Testing
# -----------------
httpx>=0.27.0               # HTTP client bất đồng bộ (async requests)
python-dotenv>=1.0.1        # Đọc biến môi trường từ file .env
pytest>=8.2.0               # Framework kiểm thử tự động (Unit test)
